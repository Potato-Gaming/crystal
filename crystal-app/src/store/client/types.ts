import { RegionLocale as _RegionLocale } from 'league-client';
import { AsyncData } from '../../utils/types';

export type RegionLocale = _RegionLocale;

export type RegionLocaleState = AsyncData<RegionLocale>;
